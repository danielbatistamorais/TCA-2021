declare type ProxyOptions = {
    hasFake: (namespace: string) => boolean;
    useFake: (namespace: string, value: any) => any;
};
/**
 * Proxies the objects to fallback to fake, when it exists.
 */
export declare class IocProxyObject {
    namespace: string;
    value: any;
    options: ProxyOptions;
    constructor(namespace: string, value: any, options: ProxyOptions);
}
/**
 * Proxies the class constructor to fallback to fake, when it exists.
 */
export declare function IocProxyClass(namespace: string, value: any, options: ProxyOptions): {
    options: ProxyOptions;
    namespace: string;
    value: any;
};
export {};
