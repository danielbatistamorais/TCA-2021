import { IocContract } from '../Contracts';
/**
 * Exposes the API to injecting dependencies to a class or a method
 */
export declare class Injector {
    private container;
    constructor(container: IocContract);
    /**
     * Resolves the injections to be injected to a method or the
     * class constructor
     */
    private resolveInjections;
    /**
     * Injects dependencies to the constructor of a class.
     */
    injectConstructorDependencies(target: any, runtimeValues: any[]): any;
    /**
     * Injects dependencies to the constructor of a class.
     */
    injectMethodDependencies(target: any, method: string, runtimeValues: any[]): any;
}
