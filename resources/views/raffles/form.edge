<div class="mb-3">
    <label for="title" class="form-label">Título</label>
    <input class="form-control mb-3" type="text" name="title" id="title" value="{{ flashMessages.get('title', raffle.title) || '' }}">

    <label for="description" class="form-label">Descrição</label>
    <input class="form-control mb-3" type="text" name="description" id="description" value="{{ flashMessages.get('description', raffle.description) || '' }}">
    
    <label for="probableDrawDate" class="form-label">Data provável do sorteio</label>
    <input class="form-control mb-3" type="datetime-local" name="probableDrawDate" id="probableDrawDate" value="{{ flashMessages.get('probableDrawDate', raffle.probableDrawDate) || '' }}">

    <div class="row">
        <div class="col-6">
            <label for="initialSaleDate" class="form-label">Data inicial da venda</label>
            <input class="form-control mb-3" type="datetime-local" name="initialSaleDate" id="initialSaleDate" value="{{ flashMessages.get('initialSaleDate', raffle.initialSaleDate) || '' }}">
        </div>
        <div class="col-6">
            <label for="endSaleDate" class="form-label">Data final da venda</label>
            <input class="form-control mb-3" type="datetime-local" name="endSaleDate" id="endSaleDate" value="{{ flashMessages.get('endSaleDate', raffle.endSaleDate) || '' }}">
        </div>                
    </div>        
    
    <label for="ticketPrize" class="form-label">Valor do bilhete</label>
    <input class="form-control mb-3" type="number" name="ticketPrize" id="ticketPrize" value="{{ flashMessages.get('ticketPrize', raffle.ticketPrize) || '' }}">
    
    <select class="form-select">
    <option disabled selected>Selecione um tipo...</option>
    @each(type in types)
        <option class="form-control mb-3" type="number" name="typeId" id="typeId" value="{{ type.id }}" @if(flashMessages.get('typeId') == type.id) selected @endif>
            {{ type.description }}</option>
    @endeach
    </select>

    @if(flashMessages.has('errors.title'))
        <small class="text-danger">{{ flashMessages.get('errors.title') }}</small>
    @endif
</div>
  